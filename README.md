# Parking API with CI/CD and Linters
Проект с линтерами (black, isort, flake8, mypy) и тестами.
Локальный запуск: 
  python -m venv venv
  venv\Scripts\activate
  pip install -r requirements-dev.txt
  pytest tests/ -v
